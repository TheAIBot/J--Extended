package pass;

public class Ternary {
	
	public int ternary(boolean trueOrFalse, int ifTrue, int ifFalse) {
		return trueOrFalse ? ifTrue : ifFalse;
	}
	
	public double ternaryD(boolean trueOrFalse, double ifTrue, double ifFalse) {
		return trueOrFalse ? ifTrue : ifFalse;
	}
}
